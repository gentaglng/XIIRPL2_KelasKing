import 'package:flutter/material.dart';

class BnbAbsen extends StatelessWidget {
  const BnbAbsen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
