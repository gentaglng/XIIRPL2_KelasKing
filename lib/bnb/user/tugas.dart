import 'package:flutter/material.dart';

class UserTugas extends StatelessWidget {
  const UserTugas({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
