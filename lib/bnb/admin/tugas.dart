import 'package:flutter/material.dart';

class AdminTugas extends StatelessWidget {
  const AdminTugas({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
