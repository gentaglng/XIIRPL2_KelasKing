import 'package:flutter/material.dart';

class AdminAbsen extends StatelessWidget {
  const AdminAbsen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
