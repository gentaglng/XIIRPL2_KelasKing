import 'package:flutter/material.dart';

class BnbAbsenAdmin extends StatelessWidget {
  const BnbAbsenAdmin({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
